/**
 * Empty locally (http://localhost:3000). Vercel sets `/demo/phmc-privilege-admin`
 * via `vercel.json` — same pattern as webapp-fms `/demo/fms`.
 */
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH ?? "";
